<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="action">
		<%= f.submit "Update" %>
	</div>
<% end %>
<br>
<div>
	<%= gravatar_for @user %>
	<a href="http://gravatar.com/emails">change</a>
</div>